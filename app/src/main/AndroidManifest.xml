<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.hudsonleao.tonyslanches">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activityPrincipal" />
        <activity android:name=".activityMenu" />
        <activity android:name=".activitySanduiche" />
        <activity android:name=".activityGarcom" />
        <activity android:name=".activityConsultar" />
        <activity android:name=".activityHistorico" />
        <activity android:name=".activityFinalizarPedido" />
        <activity android:name=".single_list_item" />
        <activity android:name=".activityCadastro"></activity>
    </application>

</manifest>